---
description: ADCMS 项目关键上下文（前端/后端职责、构建/启动、端口）
alwaysApply: true
---

# ADCMS 项目记忆

- `admin`：vben admin pro 的前端代码（前端端口 `5666`）
- `backend`：后端代码与运行脚本所在目录

## 后端构建与启动

- `backend/build.sh`：构造 Linux 运行环境（生成后端可执行文件等）
- `backend/start.sh`：停止旧进程并启动新进程
  - 会检查并强制清理占用 `8081` 端口的旧进程（`lsof -ti:8081` + `kill -9`）
  - 启动命令：`nohup ./adcms-server > logs/server.log 2>&1 &`
  - 健康检查：请求 `http://localhost:8081/health`
  - 日志：`backend/logs/server.log`

## 端口约定

- 前端（admin）：`5666`
- 后端（backend）：`8081`

操作的时候注意不要结束 node 进程防止连接断开

后端的启动命令 cd backend && ./build.sh && ./start.sh


## 用户与权限约定

- 每个用户的 `id` 都是 UUID
- 若用户 `is_admin = 1`，则该用户视为超级管理员（后端不再依赖 `is_platform_super_admin`）
- 超级管理员可以添加管理员
- 管理员可以添加用户
- 超级管理员可以看到所有菜单
- 管理员可以看到除「菜单管理」「角色管理」之外的所有菜单
- 管理员看到的数据范围：仅自己及自己创建的用户相关数据
- 员工仅可看到三个菜单：`工作台`、`表单管理`、`信息管理`

## 站点信息与数据可见范围

- 超级管理员和管理员都可以配置自己的网站信息
- 用户访问时展示其归属管理员/超级管理员配置的网站信息
- 「站群管理」「表单管理」的数据范围：
  - 超级管理员可看全部
  - 管理员仅可看自己范围内的数据
